import { FormatoPuntosPipePipe } from './formato-puntos-pipe-pipe';

describe('FormatoPuntosPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoPuntosPipePipe();
    expect(pipe).toBeTruthy();
  });
});
