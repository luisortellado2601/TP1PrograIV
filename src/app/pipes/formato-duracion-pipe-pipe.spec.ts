import { FormatoDuracionPipePipe } from './formato-duracion-pipe-pipe';

describe('FormatoDuracionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoDuracionPipePipe();
    expect(pipe).toBeTruthy();
  });
});
