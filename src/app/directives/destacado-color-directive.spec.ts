import { DestacadoColorDirective } from './destacado-color-directive';

describe('DestacadoColorDirective', () => {
  it('should create an instance', () => {
    const directive = new DestacadoColorDirective();
    expect(directive).toBeTruthy();
  });
});
