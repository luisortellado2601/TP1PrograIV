import { EdadColorDirective } from './edad-color-directive';

describe('EdadColorDirective', () => {
  it('should create an instance', () => {
    const directive = new EdadColorDirective();
    expect(directive).toBeTruthy();
  });
});
